{% extends 'base.html.twig' %}

{% block title %}Bidule{% endblock %}

{% block body %}
    <h1>Bidule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bidule.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bidule.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ bidule.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bidule_index') }}">back to list</a>

    <a href="{{ path('app_bidule_edit', {'id': bidule.id}) }}">edit</a>

    {{ include('bidule/_delete_form.html.twig') }}
{% endblock %}
